version: 2.1

jobs:
  say-hello:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: "echo Hello, World!"
      - run:
          name: "info systeme"
          command: "wget www.arkhe.io/f/m2i/gitlab/a.sh"
      - run:
          name: "exec"
          command: "chmod +x ./a.sh"
      - run:
          name: "info systeme"
          command: "./a.sh"
          
workflows:
  say-hello-workflow:
    jobs:
      - say-hello
